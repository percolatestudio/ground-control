positioning($type, $top = auto, $right = auto, $bottom = auto, $left = auto, $width = auto, $height = auto) {
  position: $type;
  top: $top;
  bottom: $bottom;
  left: $left;
  right: $right;
  width: $width;
  height: $height;
}
